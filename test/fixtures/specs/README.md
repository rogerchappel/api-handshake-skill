# Fixture Specs

These fixture notes model a small payment API with auth, endpoint, schema, webhook, retry, rate-limit, environment, and acceptance signals. They are synthetic and contain no credentials.
