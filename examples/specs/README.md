# Example Specs

These synthetic notes describe a small payments API and contain no real
credentials. Use them to try the CLI against a complete local handshake input:

```bash
node bin/api-handshake-skill.js plan examples/specs --out tmp/integration-plan.md
node bin/api-handshake-skill.js fixtures tmp/integration-plan.md --out tmp/fixtures
```
